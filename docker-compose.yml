version: '3.8'

services:
  bot:
    build: .
    container_name: skyvellum_bot
    restart: always
    volumes:
      - ./logs:/app/logs
      - .bot/database:/app/bot/database
    env_file:
      - .env
